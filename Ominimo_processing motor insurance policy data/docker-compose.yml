version: "3.9"

services:
  motor-policy-pipeline:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: motor-policy-pipeline
    volumes:
      - ./data:/app/data
    command: ["python", "main.py", "--metadata", "data/input/motor_policy.json"]
